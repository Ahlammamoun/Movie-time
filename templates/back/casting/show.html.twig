{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <h1>Casting</h1>
 <a href="{{ path('app_back_movie_index') }}" class="text-danger bg-dark"  >back to list</a>
    <table class="text-xl text-light font-weight-bold bg-dark bg-gradient w-100 p-3">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ casting.id }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ casting.role }}</td>
            </tr>
            <tr>
                <th>CreditOrder</th>
                <td>{{ casting.creditOrder }}</td>
            </tr>
        </tbody>
    </table>

   
    <a href="{{ path('app_back_casting_edit', {'id': casting.id}) }}" class="text-danger bg-dark">edit</a>

    {{ include('back/casting/_delete_form.html.twig') }}
{% endblock %}
