{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
 <a href="{{ path('app_back_movie_index') }}" class="text-danger bg-dark"  >back to list</a>
    <h1>Genre</h1>



    <table class="table text-xl text-light font-weight-bold bg-dark bg-gradient">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ genre.name }}</td>
            </tr>
        </tbody>
    </table>

    

    <a href="{{ path('app_back_genre_edit', {'id': genre.id}) }}" class="text-danger bg-dark" >edit</a>

    {{ include('back/genre/_delete_form.html.twig') }}
{% endblock %}
