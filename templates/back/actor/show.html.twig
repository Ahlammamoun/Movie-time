{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
  <a href="{{ path('app_back_actor_index') }}"  class="text-danger bg-dark" >back to list</a>
    <h1>Actor</h1>

    <table class="table text-xl text-light font-weight-bold bg-dark bg-gradient">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actor.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ actor.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ actor.lastname }}</td>
            </tr>
        </tbody>
    </table>

  

    <a href="{{ path('app_back_actor_edit', {'id': actor.id}) }}" class="text-danger bg-dark">edit</a>

    {{ include('back/actor/_delete_form.html.twig') }}
{% endblock %}
