{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
  <a href="{{ path('app_back_actor_index') }}"  class="text-info bg-dark" >back to list</a>
    <h1>Actor</h1>

    <table class="table text-xl font-weight-bold bg-secondary bg-gradient">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ actor.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ actor.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ actor.lastname }}</td>
            </tr>
        </tbody>
    </table>

  

    <a href="{{ path('app_back_actor_edit', {'id': actor.id}) }}">edit</a>

    {{ include('back/actor/_delete_form.html.twig') }}
{% endblock %}
