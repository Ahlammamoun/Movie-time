<!DOCTYPE html>
<html lang="fr">
    <head>
       <meta charset="utf-8">
      <meta name="viewport" content="width=device-width, initial-scale=1" />
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
      <link rel="stylesheet" href="../assets/css/style.css">
      <link rel="shortcut icon" href="{{ asset('/../assets/favicon.png') }}" type="image/x-icon">
   </head>
 <body>
 
{% block title %}{% endblock %}
{% if app.session.get('theme') == 'fliflix' %}
<nav class="navbar navbar-expand-lg text-light bg-dark">
{% else %}
<nav class="navbar navbar-expand-lg text-light bg-dark " >

{% endif %}

   <div class="container-fluid">
    <img src="assets/favicon.png" height="25" alt="logo" class="logo">
      <a class="navbar-brand text-danger text-uppercase font-weight-bold" href="{{ path('app_home') }}" >Movie time</a>
      <button class="navbar-toggler " type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
         <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
               <a class="nav-link active text-light" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
            </li>
            <li class="nav-item">
               <a class="nav-link text-light" href="{{ path('app_movies') }}">Films, Séries TV	
&#127909;</a>
            </li>
            <li class="nav-item ">
               <a class="nav-link text-light" href="{{ path('main_favorites') }}">Ma liste</a>
               </li>
            
      {% if app.user %}
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0 text-light">
         <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-light" href="#" id="navbarDropdown" role="button"
               data-bs-toggle="dropdown">
               <span>{{ app.user.email }} </span>
               <i class="bi bi person-square">
            </a>
            <ul class="dropdown-menu">
               <li><a class="dropdown-item text-uppercase" href="#">({{ app.user.displayRole() }})</a></li>
               <li><a class="dropdown-item" href="{{ path('app_back_movie_index')}}">Backoffice</a></li>
               <li>
                  <hr class="dropdown-divider">
               </li>
               <li><a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a></li>
            </ul>
            </li>
         <li>

  
      {% else %}
            <li>
                  <a class="nav-link btn btn-danger text-light" href="{{ path('app_login') }}">Connexion</a>
             </li>
 </ul>
      {% endif %}

         <form class="d-flex" action="html.list" >
            <input class="form-control form-control-sm me-2" name="search" type="search" placeholder="rechercher.." >
            <button class="btn btn-outline-danger" type="submit"><i class="bi bi-search"></i></button>
         </form>
      </div>
   </div>
</nav>
{% block navAdmin %}{% endblock %}
{% block body %}{% endblock %}
      <footer class="container-fluid py-4 border-top text-center">
         <span class="text-muted">Movie time made with &hearts;
         <br><small>Thème :
               <a href="{{  path('main_theme_switcher') }}" class="link-secondary text-decoration-none">
                  {% if app.session.get('theme', 'fliflix') == 'fliflix' %}
                  ociné <i class="bi bi-toggle-on mx-1"></i> fliflix
                  {% else %}
                  ociné <i class="bi bi-toggle-off mx-1"></i> fliflix
                  {% endif %}
            </a></small>
         </span>
      </footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa"crossorigin="anonymous"></script>

</html>

	


