<!DOCTYPE html>
<html lang="fr">
    <head>
       <meta charset="utf-8">
      <meta name="viewport" content="width=device-width, initial-scale=1" />
      <link  rel ="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
      <link rel="stylesheet" href="../assets/css/style.css">
      <link rel="shortcut icon" href="{{ asset('assets/favicon.png') }}" type="image/x-icon">
   </head>
 <body>
 
{% block title %}{% endblock %}
{% if app.session.get('theme') == 'fliflix' %}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
{% else %}
<nav class="navbar navbar-expand-lg navbar-light bg-warning" >

{% endif %}

   <div class="container-fluid">
      <a class="navbar-brand" href="{{ path('main_home') }}" class="d-flex align-items-center mb-2 mb-lg-0 text-white">Movie time</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
         <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
               <a class="nav-link active" aria-current="page" href="{{ path('main_home') }}">Accueil</a>
            </li>
            <li class="nav-item">
               <a class="nav-link" href="{{ path('main_list') }}">Films</a>
            </li>
            <li class="nav-item ">
               <a class="nav-link" href="{{ path('main_favorites') }}">Favories</a>
               </li>
               
         <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
               <li class="nav-item dropdown">
                     <a class="nav-link dropdown-toogle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                        <span>Charles</span>
                        <i class="bi bi-person-square"></i>
                     </a>
                     <ul class="dropdown-menu">

            
            <li><a class="dropdown-item" href="#">Profil</a></li>
                <li><a class="dropdown-item" href="#">Backoffice</a></li>
           
         
            <li> <hr class="dropdown-divider"></li>

            <li class="dropdown-item ">
               <a class="nav-link" href="#">Déconnexion</a>
               </li>
      </ul>
</li>
</ul>
 <button type="button" class="btn btn-danger me-2">Connexion</button>

         <form class="d-flex" action="html.list" >
            <input class="form-control form-control-sm me-2" name="search" type="search" placeholder="rechercher.." >
            <button class="btn btn-outline-danger" type="submit"><i class="bi bi-search"></i></button>
         </form>
      </div>
   </div>
</nav>

{% block body %}{% endblock %}
<footer class="container-fluid py-4 border-top text-center">
<span class="text-muted">Movie time made with &hearts;
<br><small>Thème :
   <a href="{{  path('main_theme_switcher') }}" class="link-secondary text-decoration-none">
      {% if app.session.get('theme', 'fliflix') == 'fliflix' %}
      ociné <i class="bi bi-toggle-on mx-1"></i> fliflix
      {% else %}
      ociné <i class="bi bi-toggle-off mx-1"></i> fliflix
      {% endif %}
</a></small>
</span>
</footer>

   </body>
</html>

	<!--<script src= "https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" >-->


